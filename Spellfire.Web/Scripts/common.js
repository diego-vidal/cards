var Spellfire = window.Spellfire = window.Spellfire || {};

Spellfire.Common =
    (function (module, $) {

        "use strict";

        // Public
        module.init = function () {

        };

        // Private
        var self = {

        };

        return module;

    })(Spellfire.Common || {}, window.jQuery);
